{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Introducing MapManagerCore","text":"<p>This is mapmanagercore</p>"},{"location":"api/metadata/","title":"Metadata","text":"<p>Metadata stores information about images. It provides a single timepoint image with multiple color channels, and a list of single timepoint images.</p> <p>Classes:</p> Name Description <code>mmMapMetadata</code> <p>A list of <code>TimepointMetaData</code> representing a collection of single time point images.</p> <code>TimepointMetadata</code> <p>Metadata for a single timepoint with multiple channels.</p> <code>ChannelMetadata</code> <p>The metadata for one channel.</p> <p>Example:</p> <pre><code># make some miage data\nimgData = np.random.randint(low=0, high=2**11, size=(20,512,512), dtype=np.uint16)\n\n# creat a timepoint and append a channel of image data.\ntpmd = TimepointMetadata()\ntpmd.appendChannel(imgData)\n\n# append the timepoint to a list of timepoints\nmapmd = mmMapMetadata()\nmapmd.appendTimepoint(tpmd)\n\nprint(mapmd.numTimepoint)  # -&gt; 1\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3-classes","title":"Classes","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams","title":"<code>AnalysisParams</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_metadataBase</code></p> <p>Analysis parameters for a single timepoint (also for all timepoints in a mmap).</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass AnalysisParams(_metadataBase):\n    \"\"\"Analysis parameters for a single timepoint (also for all timepoints in a mmap).\n    \"\"\"\n    version:float = 0.6\n    \"\"\"Manually increment this when we add to this class.\"\"\"\n\n    # spines\n    brightestPathDistance: int = dataclasses.field(default=10, metadata={'description': \n                                                             'Points along the tracing to find spine connection (anchor).'\n                                                             })\n    brightestPathChannel: int = dataclasses.field(default=0, metadata={'description': \n                                                             'Image color channel to find brightest connection of spine.'\n                                                             })\n    brightestPathZSpread: int = dataclasses.field(default=3, metadata={'description': \n                                                             'Number of image slices for max project to find brightest connection of spine.'\n                                                             })\n    roiExtend: int = dataclasses.field(default=4, metadata={'description': \n                                                             'Number of pixels to extend spine head for spine ROI.'\n                                                             })\n    roiRadius: int = dataclasses.field(default=4, metadata={'description': \n                                                             'Width of spine ROI.'\n                                                             })\n\n    # segments\n    segmentRadius: int = dataclasses.field(default=4, metadata={'description': \n                                                             'Radius of segment tracing.'\n                                                             })\n    segmentTracingMaxDistance: int = dataclasses.field(default=90, metadata={'description': \n                                                             'Max distance to trace a brightest path.'\n                                                             })\n    backgroundRoiGridPoints: int = dataclasses.field(default=5, metadata={'description': \n                                                             'Number of points in grid (nxn) to calculate background ROI.'\n                                                             })\n    backgroundRoiGridOverlap: float = dataclasses.field(default=0.1, metadata={'description': \n                                                             'Overlap of background grid points.'\n                                                             })\n\n    def getDescription(self, attribute_name:str) -&gt; str:\n        \"\"\"Get the 'description' from one attribute metadata.\n        \"\"\"\n        return self.__dataclass_fields__[attribute_name].metadata['description']\n\n    def printFields(self):\n        \"\"\"debugging.\n        \"\"\"\n        for oneField in dataclasses.fields(self):\n            # print(f'oneField:{oneField}')\n\n            name = oneField.name\n            _type = oneField.type\n\n            value = self.getValue(name)\n            default = oneField.default\n            try:\n                metadata = oneField.metadata\n            except (KeyError) as e:\n                metadata = ''\n            print(name, value, _type, default, metadata)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams-attributes","title":"Attributes","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams.version","title":"<code>version: float = 0.6</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Manually increment this when we add to this class.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams-functions","title":"Functions","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams.getDescription","title":"<code>getDescription(attribute_name: str) -&gt; str</code>","text":"<p>Get the 'description' from one attribute metadata.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def getDescription(self, attribute_name:str) -&gt; str:\n    \"\"\"Get the 'description' from one attribute metadata.\n    \"\"\"\n    return self.__dataclass_fields__[attribute_name].metadata['description']\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.AnalysisParams.printFields","title":"<code>printFields()</code>","text":"<p>debugging.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def printFields(self):\n    \"\"\"debugging.\n    \"\"\"\n    for oneField in dataclasses.fields(self):\n        # print(f'oneField:{oneField}')\n\n        name = oneField.name\n        _type = oneField.type\n\n        value = self.getValue(name)\n        default = oneField.default\n        try:\n            metadata = oneField.metadata\n        except (KeyError) as e:\n            metadata = ''\n        print(name, value, _type, default, metadata)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata","title":"<code>ChannelMetadata</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_metadataBase</code></p> <p>Metadata for one color channel.</p> <p>When we import,     We have the full image volume and store (minInt, maxInt, dtype).</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass ChannelMetadata(_metadataBase):\n    \"\"\"Metadata for one color channel.\n\n    When we import,\n        We have the full image volume and store (minInt, maxInt, dtype).\n    \"\"\"\n\n    minInt: int = 1\n    \"\"\"Min intensity of image data, set on import (see _initFromImgData) then immutable.\"\"\"\n    maxInt: int = 1\n    \"\"\"Max intensity of image data, set on import then immutable.\"\"\"\n    dtype: str = 'Unknown'\n    \"\"\"String representation of dtype\"\"\"\n    #\n    # the remaining fields can be set by the user.\n    minContrast: int = 1\n    maxContrast: int = 1\n    color : str = \"x00FF00\"  # map -&gt; 'green'\n    \"\"\"Color LUT for the image.\"\"\"\n\n    name: str = 'Untitled'\n    \"\"\"Name of the channel.\"\"\"\n\n    def _initFromImgData(self, imgData : np.ndarray):\n        \"\"\"On import of an image.\n\n        This assumes we have the entire image volume.\n        \"\"\"\n        self.minInt = int(np.min(imgData))\n        self.maxInt = int(np.max(imgData))\n        minContrast, maxContrast = getAutoContrast(imgData)\n        self.minContrast = minContrast\n        self.maxContrast = maxContrast\n\n        self.dtype = imgData.dtype.name\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata-attributes","title":"Attributes","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata.color","title":"<code>color: str = 'x00FF00'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Color LUT for the image.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata.dtype","title":"<code>dtype: str = 'Unknown'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>String representation of dtype</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata.maxInt","title":"<code>maxInt: int = 1</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Max intensity of image data, set on import then immutable.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata.minInt","title":"<code>minInt: int = 1</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Min intensity of image data, set on import (see _initFromImgData) then immutable.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.ChannelMetadata.name","title":"<code>name: str = 'Untitled'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Name of the channel.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.ExperimentMetadata","title":"<code>ExperimentMetadata</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_metadataBase</code></p> <p>For each image acquired, user can specify <code>Experiment</code> metadata.</p> <p>This spans all color channels.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass ExperimentMetadata(_metadataBase):\n    \"\"\"For each image acquired, user can specify `Experiment` metadata.\n\n    This spans all color channels.\n    \"\"\"\n    Species: str = ''\n    Sex: str = ''\n    Age: str = ''\n    Region: str = ''\n    CellType: str = ''\n    AcqDate: str = ''\n    AcqTime: str = ''\n    Condition1: str = ''\n    Condition2: str = ''\n    Timer1: str = ''\n    Timer2: str = ''\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.ShapeMetadata","title":"<code>ShapeMetadata</code>  <code>dataclass</code>","text":"<p>The shape of the image data in pixels.</p> <p>Set on import and never changes.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass ShapeMetadata:\n    \"\"\"The shape of the image data in pixels.\n\n    Set on import and never changes.\n    \"\"\"\n    xPixels: int = 1\n    yPixels: int = 1\n    zPixels: int = 1\n\n    @property\n    def shape(self):\n        return (self.zPixels, self.yPixels, self.xPixels)\n\n    def _initFromImgData(self, imgData : np.ndarray):\n        \"\"\"Set shape on import.\n\n        After import, never changes.\n        \"\"\"\n        proposedShape = imgData.shape\n        if len(proposedShape)==2:\n            # single plane image\n            self.xPixels = proposedShape[1]\n            self.yPixels = proposedShape[0]\n            self.zPixels = 1\n        elif len(proposedShape)==3:\n            self.zPixels = proposedShape[0]\n            self.xPixels = proposedShape[2]\n            self.yPixels = proposedShape[1]\n        else:\n            logger.mmlog('expecting img data of shape len 2 or 3, got {proposedShape}')\n            return\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata","title":"<code>TimepointMetadata</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_metadataList</code></p> <p>Metadata for one timepoint.</p> <p>In our single timepoint analysis, each timepoint is independent of all others.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>User defined name of the timepoint.</p> <code>'Untitled'</code> TODO <p>When we are in a multi-timepoint map (with segments and spine connect) we need one global AnalysisParameters (At root of zarr file)</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass TimepointMetadata(_metadataList):\n    \"\"\"Metadata for one timepoint.\n\n    In our single timepoint analysis, each timepoint is independent of all others.\n\n    Args:\n        name: User defined name of the timepoint.\n\n    TODO:\n        When we are in a multi-timepoint map (with segments and spine connect)\n        we need one global AnalysisParameters (At root of zarr file)\n    \"\"\"\n\n    name: str = 'Untitled'\n\n    shapeMetadata: ShapeMetadata = dataclasses.field(default_factory=lambda: ShapeMetadata())\n    \"\"\"The shape of the raw image data (once loaded with raw data, this is never changed).\"\"\"\n\n    voxelMetadata: VoxelMetadata = dataclasses.field(default_factory=lambda: VoxelMetadata())\n    \"\"\"The physical unit voxel size in each dimension.\"\"\"\n\n    experimentMetadata : ExperimentMetadata = dataclasses.field(default_factory=lambda: ExperimentMetadata())\n    \"\"\"Experimental metadata corresponding to the conditions an image was aquired.\"\"\"\n\n    analysisParameters: AnalysisParams = dataclasses.field(default_factory=lambda: AnalysisParams())\n    \"\"\"The analysis parameters for this single timepoint (connected maps use a global version of this).\"\"\"\n\n    def appendChannel(self, imgData : np.ndarray) -&gt; Optional[int]:\n        \"\"\"Given img data, append a new color channel. Used when we are importing data.\n\n        Parameters:\n            imgData: The image data to append.\n\n        Returns:\n            Appended channel index on success, otherwise None.\n\n        Notes:\n            Will fail if appending channel index &gt; 0 and\n            proposed channel imgData.shape does not match channel index 0 shape.\n        \"\"\"\n\n        proposedShape = imgData.shape\n        if self.numChannels == 0:\n            # always append channel 0,\n            # members like self.xPixels will never change\n            self.shapeMetadata._initFromImgData(imgData)\n\n        else:\n            if self.numChannels &gt; 0:\n                # check proposedShape\n                if proposedShape != self.shape:\n                    logger.mmlog(f'expecting shape {self.shape} but got {proposedShape}')\n                    return\n\n        metadataContrast = ChannelMetadata()\n        metadataContrast._initFromImgData(imgData)\n\n        # do the append\n        newChannelIndex = self.appendMetadataItem(metadataContrast)\n        return newChannelIndex\n\n    def deleteChannel(self, channelIdx:int) -&gt; Optional[ChannelMetadata]:\n        \"\"\"Delete a color channel.\n\n        Returns:\n            On success, deleted ChannelMetadata, otherwise None\n        \"\"\"\n        if channelIdx == 0:\n            logger.mmlog('cannot delete channel 0')\n            return\n\n        metadataContrast = self.deleteMetadataItem(channelIdx)\n        return metadataContrast\n\n    def swapChannels(self, srcChannelIdx, dstChannelIdx) -&gt; bool:\n        \"\"\"Move/swap color channel.\n        \"\"\"\n        ok = self.swapMetadataItems(srcChannelIdx, dstChannelIdx)\n        return ok\n\n    def getChannelMetadata(self, channelIdx) -&gt; Optional[ChannelMetadata]:\n        \"\"\"Get metadata for a channel.\n        \"\"\"\n        return self.getMetadataItem(channelIdx)\n\n    @property\n    def numChannels(self) -&gt; int:\n        \"\"\"The number of channels in the timepoint.\n        \"\"\"\n        return self.numItems\n\n    @property\n    def shape(self) -&gt; Tuple[int,int,int]:\n        \"\"\"The shape of the image data in the timepoint.\n\n        Notes\n        -----\n        All channels in a timepoint have the same shape.\n        \"\"\"\n        return self.shapeMetadata.shape\n\n    def setChannelProperty(self, channelIdx, key, value):\n        \"\"\"Set channel property.\n        \"\"\"\n        if channelIdx not in self.listIndices:\n            return\n        return self._metadataList[channelIdx].setValue(key, value)\n\n    def getChannelProperty(self, channelIdx, key):\n        \"\"\"Get channel property.\n        \"\"\"\n        if channelIdx not in self.listIndices:\n            return\n        return self._metadataList[channelIdx].getValue(key)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata-attributes","title":"Attributes","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.analysisParameters","title":"<code>analysisParameters: AnalysisParams = dataclasses.field(default_factory=lambda: AnalysisParams())</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The analysis parameters for this single timepoint (connected maps use a global version of this).</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.experimentMetadata","title":"<code>experimentMetadata: ExperimentMetadata = dataclasses.field(default_factory=lambda: ExperimentMetadata())</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Experimental metadata corresponding to the conditions an image was aquired.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.numChannels","title":"<code>numChannels: int</code>  <code>property</code>","text":"<p>The number of channels in the timepoint.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.shape","title":"<code>shape: Tuple[int, int, int]</code>  <code>property</code>","text":"<p>The shape of the image data in the timepoint.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.shape--notes","title":"Notes","text":"<p>All channels in a timepoint have the same shape.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.shapeMetadata","title":"<code>shapeMetadata: ShapeMetadata = dataclasses.field(default_factory=lambda: ShapeMetadata())</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The shape of the raw image data (once loaded with raw data, this is never changed).</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.voxelMetadata","title":"<code>voxelMetadata: VoxelMetadata = dataclasses.field(default_factory=lambda: VoxelMetadata())</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The physical unit voxel size in each dimension.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata-functions","title":"Functions","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.appendChannel","title":"<code>appendChannel(imgData: np.ndarray) -&gt; Optional[int]</code>","text":"<p>Given img data, append a new color channel. Used when we are importing data.</p> <p>Parameters:</p> Name Type Description Default <code>imgData</code> <code>ndarray</code> <p>The image data to append.</p> required <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Appended channel index on success, otherwise None.</p> Notes <p>Will fail if appending channel index &gt; 0 and proposed channel imgData.shape does not match channel index 0 shape.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def appendChannel(self, imgData : np.ndarray) -&gt; Optional[int]:\n    \"\"\"Given img data, append a new color channel. Used when we are importing data.\n\n    Parameters:\n        imgData: The image data to append.\n\n    Returns:\n        Appended channel index on success, otherwise None.\n\n    Notes:\n        Will fail if appending channel index &gt; 0 and\n        proposed channel imgData.shape does not match channel index 0 shape.\n    \"\"\"\n\n    proposedShape = imgData.shape\n    if self.numChannels == 0:\n        # always append channel 0,\n        # members like self.xPixels will never change\n        self.shapeMetadata._initFromImgData(imgData)\n\n    else:\n        if self.numChannels &gt; 0:\n            # check proposedShape\n            if proposedShape != self.shape:\n                logger.mmlog(f'expecting shape {self.shape} but got {proposedShape}')\n                return\n\n    metadataContrast = ChannelMetadata()\n    metadataContrast._initFromImgData(imgData)\n\n    # do the append\n    newChannelIndex = self.appendMetadataItem(metadataContrast)\n    return newChannelIndex\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.deleteChannel","title":"<code>deleteChannel(channelIdx: int) -&gt; Optional[ChannelMetadata]</code>","text":"<p>Delete a color channel.</p> <p>Returns:</p> Type Description <code>Optional[ChannelMetadata]</code> <p>On success, deleted ChannelMetadata, otherwise None</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def deleteChannel(self, channelIdx:int) -&gt; Optional[ChannelMetadata]:\n    \"\"\"Delete a color channel.\n\n    Returns:\n        On success, deleted ChannelMetadata, otherwise None\n    \"\"\"\n    if channelIdx == 0:\n        logger.mmlog('cannot delete channel 0')\n        return\n\n    metadataContrast = self.deleteMetadataItem(channelIdx)\n    return metadataContrast\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.getChannelMetadata","title":"<code>getChannelMetadata(channelIdx) -&gt; Optional[ChannelMetadata]</code>","text":"<p>Get metadata for a channel.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def getChannelMetadata(self, channelIdx) -&gt; Optional[ChannelMetadata]:\n    \"\"\"Get metadata for a channel.\n    \"\"\"\n    return self.getMetadataItem(channelIdx)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.getChannelProperty","title":"<code>getChannelProperty(channelIdx, key)</code>","text":"<p>Get channel property.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def getChannelProperty(self, channelIdx, key):\n    \"\"\"Get channel property.\n    \"\"\"\n    if channelIdx not in self.listIndices:\n        return\n    return self._metadataList[channelIdx].getValue(key)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.setChannelProperty","title":"<code>setChannelProperty(channelIdx, key, value)</code>","text":"<p>Set channel property.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def setChannelProperty(self, channelIdx, key, value):\n    \"\"\"Set channel property.\n    \"\"\"\n    if channelIdx not in self.listIndices:\n        return\n    return self._metadataList[channelIdx].setValue(key, value)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.TimepointMetadata.swapChannels","title":"<code>swapChannels(srcChannelIdx, dstChannelIdx) -&gt; bool</code>","text":"<p>Move/swap color channel.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def swapChannels(self, srcChannelIdx, dstChannelIdx) -&gt; bool:\n    \"\"\"Move/swap color channel.\n    \"\"\"\n    ok = self.swapMetadataItems(srcChannelIdx, dstChannelIdx)\n    return ok\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.VoxelMetadata","title":"<code>VoxelMetadata</code>  <code>dataclass</code>","text":"<p>The metadata for the physical size of a voxel.</p> <p>Parameters:</p> Name Type Description Default <code>xVoxel</code> <code>float</code> <p>The x-coordinate's voxel size.</p> <code>1.0</code> <code>yVoxel</code> <code>float</code> <p>The y-coordinate's voxel size.</p> <code>1.0</code> <code>zVoxel</code> <code>float</code> <p>The z-coordinate's voxel size.</p> <code>1.0</code> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass VoxelMetadata:\n    \"\"\"The metadata for the physical size of a voxel.\n\n    Args:\n        xVoxel: The x-coordinate's voxel size.\n        yVoxel: The y-coordinate's voxel size.\n        zVoxel: The z-coordinate's voxel size.\n    \"\"\"\n    xVoxel: float = 1.0\n    yVoxel: float = 1.0\n    zVoxel: float = 1.0\n    unit: Literal[\"\u00b5m\"] = \"micrometer\"  # abb from \u00b5m\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata","title":"<code>mmMapMetadata</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_metadataList</code></p> <p>A list of TimepointMetadata.</p> <p>For single timepoint mmap/zarr, represents a list of independent imaging timepoints.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>@dataclasses.dataclass\nclass mmMapMetadata(_metadataList):\n    \"\"\"A list of TimepointMetadata.\n\n    For single timepoint mmap/zarr, represents a list of independent imaging timepoints.\n    \"\"\"\n\n    def getTimepointMetadata(self, index : int) -&gt; Optional[TimepointMetadata]:\n        \"\"\"Get metadata for a timepoint.\n\n        Args:\n            index: Timepoint index to get.\n\n        Returns:\n            Metadata for timepoint at index.\n        \"\"\"\n        return self.getMetadataItem(index)\n\n    def insertTimepoint(self, index : int, metadata : TimepointMetadata) -&gt; bool:\n        \"\"\"Insert a new timepoint.\n\n        Args:\n            index: The index to insert into.\n            metadata: The TimepointMetadata to insert.\n\n        Returns:\n            True on success, otherwise False.\n        \"\"\"\n        if index not in self.listIndices:\n            logger.mmlog(f'src {index} does not exist, available indices are {self.listIndices}')\n            return False\n        self.insertMetadataItem(index, metadata)\n        return True\n\n    def appendTimepoint(self, timepointMetadata : TimepointMetadata):\n        \"\"\"Append a new timepoint.\n\n        Args:\n            timepointMetadata: The timepoint to append.\n        \"\"\"\n        self.appendMetadataItem(timepointMetadata)\n\n    def deleteTimepoint(self, index : int) -&gt; Optional[TimepointMetadata]:\n        if index in self.listIndices:\n            return self.deleteMetadataItem(index)\n\n    def swapTimepoint(self, srcTimepoint : int, dstTimepoint : int) -&gt; bool:\n        \"\"\"Move/swap timepoints.\n\n        Returns:\n            True on success, otherwise False\n        \"\"\"\n        ok = self.swapMetadataItems(srcTimepoint, dstTimepoint)\n        return ok\n\n    @property\n    def numTimepoints(self) -&gt; int:\n        \"\"\"The number of timepoints.\n\n        Returns:\n            The number of timepoints.\n        \"\"\"\n        return self.numItems\n\n    def asDict(self) -&gt; dict:\n        return dataclasses.asdict(self)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata-attributes","title":"Attributes","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata.numTimepoints","title":"<code>numTimepoints: int</code>  <code>property</code>","text":"<p>The number of timepoints.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of timepoints.</p>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata-functions","title":"Functions","text":""},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata.appendTimepoint","title":"<code>appendTimepoint(timepointMetadata: TimepointMetadata)</code>","text":"<p>Append a new timepoint.</p> <p>Parameters:</p> Name Type Description Default <code>timepointMetadata</code> <code>TimepointMetadata</code> <p>The timepoint to append.</p> required Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def appendTimepoint(self, timepointMetadata : TimepointMetadata):\n    \"\"\"Append a new timepoint.\n\n    Args:\n        timepointMetadata: The timepoint to append.\n    \"\"\"\n    self.appendMetadataItem(timepointMetadata)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata.getTimepointMetadata","title":"<code>getTimepointMetadata(index: int) -&gt; Optional[TimepointMetadata]</code>","text":"<p>Get metadata for a timepoint.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Timepoint index to get.</p> required <p>Returns:</p> Type Description <code>Optional[TimepointMetadata]</code> <p>Metadata for timepoint at index.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def getTimepointMetadata(self, index : int) -&gt; Optional[TimepointMetadata]:\n    \"\"\"Get metadata for a timepoint.\n\n    Args:\n        index: Timepoint index to get.\n\n    Returns:\n        Metadata for timepoint at index.\n    \"\"\"\n    return self.getMetadataItem(index)\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata.insertTimepoint","title":"<code>insertTimepoint(index: int, metadata: TimepointMetadata) -&gt; bool</code>","text":"<p>Insert a new timepoint.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>The index to insert into.</p> required <code>metadata</code> <code>TimepointMetadata</code> <p>The TimepointMetadata to insert.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True on success, otherwise False.</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def insertTimepoint(self, index : int, metadata : TimepointMetadata) -&gt; bool:\n    \"\"\"Insert a new timepoint.\n\n    Args:\n        index: The index to insert into.\n        metadata: The TimepointMetadata to insert.\n\n    Returns:\n        True on success, otherwise False.\n    \"\"\"\n    if index not in self.listIndices:\n        logger.mmlog(f'src {index} does not exist, available indices are {self.listIndices}')\n        return False\n    self.insertMetadataItem(index, metadata)\n    return True\n</code></pre>"},{"location":"api/metadata/#mapmanagercore.metadata3.mmMapMetadata.swapTimepoint","title":"<code>swapTimepoint(srcTimepoint: int, dstTimepoint: int) -&gt; bool</code>","text":"<p>Move/swap timepoints.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True on success, otherwise False</p> Source code in <code>mapmanagercore/metadata3.py</code> <pre><code>def swapTimepoint(self, srcTimepoint : int, dstTimepoint : int) -&gt; bool:\n    \"\"\"Move/swap timepoints.\n\n    Returns:\n        True on success, otherwise False\n    \"\"\"\n    ok = self.swapMetadataItems(srcTimepoint, dstTimepoint)\n    return ok\n</code></pre>"}]}